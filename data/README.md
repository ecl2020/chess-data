fideTSV contains the text files produced by combine-weeks.py. 
ratings_lists contains the raw files downloaded directly from Mark's website. In these files, 
the data is separated by variable whitespace.
